services:
  paperless-cam:
    build: .
    container_name: paperless-cam
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    devices:
      - "/dev/video0:/dev/video0"
    volumes:
      - "./stage:/data/stage"
      - "./consume:/consume"
